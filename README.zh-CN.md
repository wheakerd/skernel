# skernel - Super-Kernel 工具

## 介绍

`skernel` 是一款功能强大的工具，旨在简化为 Super Kernel 框架项目构建 PHP 二进制文件和 PHAR 归档文件的过程。

它提供了一个简单的命令行界面 (CLI) 来处理构建任务，并支持一系列选项以适应不同的使用场景。

---

## 🚀 特征

- **可执行二进制文件**: 无需安装 PHP，即可为 Super Kernel 框架项目生成可直接运行的二进制文件。
- **PHAR 档案生成**: 为 Super Kernel 框架项目生成 PHAR 归档文件，简化部署和分发。
- **自动装弹器**: 生成的 PHAR 文件包含一个自动加载器，可实现项目的无缝部署。
- **类图和注释**: 自动为您的项目生成类图，并移除注解信息以消除冗余工作。
- **跳过文件解析错误**: 跳过文件解析错误以避免中断，尤其是在处理非关键问题时。
- **加载开发依赖项**: 允许您加载开发依赖项，以提高开发环境的灵活性。

## ⚡ 性能改进

- 提高了工具执行效率，减少了归档内存使用量，提高了整体性能。
- 优化了自动加载器和类图生成过程，以提供更快、更准确的结果。

## 🛠 安装

对于 Debian 系：

```bash
sudo rpm -ivh ./skernel.rpm
```

对于 Red Hat 系：

```bash
sudo rpm -ivh ./skernel.rpm
```

## 📢 示例

### 可用命令:

```text
build Builds a binary or PHAR archive.

Usage:
  build [options]

Options:
      --disable-binary  Disable binary build, only build the PHAR archive.
      --dev             Use `require-dev` requirements from `composer.json`.
      --debug           Enable debug mode to provide more detailed logs.
```

## 📋 License

The `skernel` is open-source software licensed under the MIT license.