<div align="center">

# 🧠 skernel

**Super-Kernel 官方构建工具**

[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)
[![PHP](https://img.shields.io/badge/PHP-%3E%3D8.4-777bb4.svg?logo=php)](https://www.php.net)
[![Super-Kernel](https://img.shields.io/badge/framework-super--kernel-blue.svg)](https://github.com/wheakerd/super-kernel)
[![Platform](https://img.shields.io/badge/platform-linux-lightgrey.svg?logo=linux)](https://www.kernel.org)

> 构建统一、执行确定、杜绝混沌。  
> **skernel 是 Super-Kernel 框架的官方构建与引导工具。**

</div>

---

## 🧩 简介

`skernel` 是为 **Super-Kernel 框架** 专属设计的构建与引导工具，旨在：

- **统一运行形态**：任何执行必须来自构建产物。
- **强化开发规范**：面向 OOP / AOP 编程，拒绝过程式实现。
- **消除运行时不确定性**：所有逻辑均在构建阶段完成。

它可为项目生成：

- ⚙️ 可执行的 PHP 二进制文件
- 📦 PHAR 归档文件

---

## 🧭 核心原则

- **杜绝运行时代码生产**： 所有代码与元信息必须在构建阶段确定，运行时禁止任何动态生成与修改。
- **面向 OOP / AOP 编程**： 开发应遵循对象与切面导向设计，禁止过程式逻辑与全局状态操作。
- **统一引导流程**： 一切执行都需经由 `skernel` 产物的引导流程。项目中不得自行接管入口、配置或引导逻辑。
- **严格 PHP 配置边界**： PHP 行为必须通过 **php.ini** 定义。任何运行时代码中调用 `ini_set()`、`error_reporting()`
  等配置函数的行为均为违规。
- **Super-Kernel 框架专属支持**：`skernel` 在构建阶段自动完成：类图扫描与索引、注解收集与 AOP 切面织入、统一加载器生成与引导文件注册、构建产物一致性验证
- **移除注解、精简追踪**： 构建产物移除注解说明，以降低体积与加速启动。异常与日志需通过 **类名追踪来源**，不依赖文件路径。

---

## ⚙️ 功能特性

| 功能             | 说明                  |
|----------------|---------------------|
| 🧱 **统一构建产物**  | 构建可执行二进制与 PHAR 归档文件 |
| 🔗 **自动引导系统**  | 自动生成统一入口和加载流程       |
| 🧭 **AOP 支持**  | 自动注解扫描与切面织入         |
| 🪶 **移除注释与注解** | 降低体积，提高执行效率         |
| 💥 **配置隔离**    | 所有配置通过 php.ini 管理   |
| 🧩 **类图与依赖生成** | 自动生成项目类图与依赖关系       |
| 🛡 **安全可控**    | 禁止运行时代码注入与配置变更      |

---

## ⚡ 性能优化

- 构建阶段性能显著提升
- PHAR 归档内存占用减少
- 类图与扫描机制并行化处理
- 跳过非关键文件错误，保证构建持续稳定

---

## 🧰 安装指南

### 从 GitHub Releases 页面下载安装

请访问：

👉 [**wheakerd/skernel - Releases**](https://github.com/wheakerd/skernel/releases)

下载与你的系统架构对应的二进制文件（如 `skernel-x86_64` 或 `skernel-aarch64`），然后执行：

```bash
chmod +x skernel
sudo mv skernel /usr/local/bin/
```

验证安装：

```bash
skernel
```

## 📢 示例

### 可用命令:

```text
build Builds a binary or PHAR archive.

Usage:
  build [options]

Options:
      --disable-binary  Disable binary build, only build the PHAR archive.
      --dev             Use `require-dev` requirements from `composer.json`.
      --debug           Enable debug mode to provide more detailed logs.
```

## 📋 许可证

本项目基于 `MIT License` 开源发布。
可自由使用、修改与分发，但需保留原始版权声明。

## 💡 设计哲学

> “框架之上，不容混沌。”
>
> skernel 不是构建工具，而是 Super-Kernel 世界观的守护者。
>
> 它拒绝动态、消除不确定，只留下可复现、可验证、可掌控的运行世界。
>
> 一切未被构建产物允许的逻辑，皆为越界。

<div align="center">
🌀 skernel 由 <a href="https://github.com/wheakerd" target="_blank">wheakerd</a> 开发与维护。  
<br />
访问 <a href="https://github.com/wheakerd/skernel" target="_blank">https://github.com/wheakerd/skernel</a> 获取更多信息。
</div>